<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Dev entitlements (ad-hoc sign). Same as entitlements.plist plus
         disable-library-validation, because hardened runtime + ad-hoc sign
         requires both binary and dylib to be signed by the same identity,
         and ad-hoc signatures don't constitute "the same identity" to
         macOS library-validation. Production builds (with a real
         Developer ID cert) use entitlements.plist instead. -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
