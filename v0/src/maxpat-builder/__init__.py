"""maxpat-builder — programmatic .maxpat / .amxd generation for StemForge.

Note: the directory name contains a dash so it cannot be imported as a normal
Python package. Use the scripts at the directory root (builder.py,
amxd_pack.py, build_amxd.py) directly, or `sys.path.insert` that directory
first. The tests in `tests/` do this via conftest.py.
"""
